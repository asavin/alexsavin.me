mixin postList(posts)
  each post in posts
    div.post-container
      a.header-link(href=post.url)
        h3.title!= post.title
      div!= post.preview
      if post.readMore
        div.read-more
          a(href=post.url) Read more...
